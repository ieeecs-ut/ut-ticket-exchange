/* MODULE – APPLICATION */
// main application logic

/* IMPORTS */
const fs = require("fs");
const path = require("path");
const utils = require("./utils");

/* MODULE */
var app; app = {
    init: _ => {

    },
    main: _ => {

    }
};
module.exports = app;